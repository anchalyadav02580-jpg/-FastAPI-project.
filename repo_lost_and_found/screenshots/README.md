# Screenshots

Place your proof-of-work screenshots here, one per required case:

1. `01_post_items.png` — POST /items (successful creation)
2. `02_get_items.png` — GET /items (list all)
3. `03_get_item_by_id.png` — GET /items/{item_id}
4. `04_put_item.png` — PUT /items/{item_id}
5. `05_delete_item.png` — DELETE /items/{item_id}
6. `06_status_filter.png` — GET /items/status/{status}
7. `07_category_filter.png` — GET /items/category/{category}
8. `08_validation_error.png` — an invalid request (e.g. empty title) showing the 422 response

Each screenshot should clearly show the endpoint/URL, the request body (if any), and the
response body + status code — the Swagger UI at `/docs` is the easiest way to capture all
three in one shot.
